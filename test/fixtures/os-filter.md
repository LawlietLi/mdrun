# Cross-platform build

```bash cmd=build os=linux,mac desc=Build on Unix
echo "Building with bash..."
cargo build
```

```powershell cmd=build os=windows desc=Build on Windows
Write-Host "Building with PowerShell..."
cargo build
```
